# Dev environment values for docker/docker-compose.yml
# Usage: docker compose --env-file .env.dev -p mssbase-dev up -d --build
ENVIRONMENT=dev
ASPNET_ENVIRONMENT=Development
SA_PASSWORD=Test12345
SECURITY_DB_NAME=SecurityDB_DEV
SQL_PORT=1433
REDIS_PORT=6379
API_PORT=8080
