info:
  name: Delete User
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{BaseUrl}}/Security/User/:UserId"
  params:
    - name: UserId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
