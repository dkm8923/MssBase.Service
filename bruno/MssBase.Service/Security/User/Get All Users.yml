info:
  name: Get All Users
  type: http
  seq: 1

http:
  method: GET
  url: "{{BaseUrl}}/Security/User"
  params:
    - name: includeInactive
      value: "true"
      type: query
      disabled: true
    - name: includeRelated
      value: "true"
      type: query
      disabled: true
    - name: IncludeReadOnly
      value: "true"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
