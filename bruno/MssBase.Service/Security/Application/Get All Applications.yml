info:
  name: Get All Applications
  type: http
  seq: 1

http:
  method: GET
  url: "{{BaseUrl}}/Security/Application"
  params:
    - name: includeInactive
      value: "true"
      type: query
      disabled: true
    - name: includeRelated
      value: "true"
      type: query
      disabled: true
    - name: IncludeReadOnly
      value: "true"
      type: query
      disabled: true
    - name: DeleteCache
      value: "true"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
