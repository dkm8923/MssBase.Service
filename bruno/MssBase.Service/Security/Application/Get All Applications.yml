info:
  name: Get All Applications
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:5217/api/security/application
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
